================================================================
KRONIA NURSE — PROMPT MASTER DE TESTES (SESSÃO COM REDE)
Execute os Blocos 0–9 da Biblioteca Técnica
Atualizado em: 2026-07-01 (3ª geração — lições da sessão anterior)
================================================================

CONTEXTO
--------
O pipeline completo da Biblioteca Técnica está implementado e
mergeado em main (klebercdc/kronia-nurse). TypeScript: 0 erros.
Schema Supabase: verificado (Bloco 0 PASS, via MCP, 2026-07-01).

Duas tentativas anteriores de rodar os Blocos 1–9 falharam, ambas
por FALTA DE ACESSO DE REDE da sessão do Claude Code aos domínios:
  uguxeoftfnljrxhwvdkj.supabase.co
  api.groq.com
  api.cohere.com

Esta sessão só serve para executar os Blocos 1–9 SE, e somente se,
o ambiente (environment) tiver sido criado com Network Access =
"Custom" (ou "Full") incluindo esses 3 domínios. Isso é configurado
na TELA DE CRIAÇÃO/EDIÇÃO DO AMBIENTE (ícone de nuvem → editar
ambiente → Network access), e só tem efeito em sessões criadas
DEPOIS da mudança ser salva. Se você está lendo isto numa sessão
já em andamento e os testes de rede abaixo (PASSO 0) falharem,
PARE — não adianta insistir, é preciso abrir uma sessão nova.


================================================================
PASSO 0 — TESTE DE REDE (fazer ANTES de qualquer outra coisa)
================================================================

  for host in api.groq.com api.cohere.com uguxeoftfnljrxhwvdkj.supabase.co; do
    echo "=== $host ==="
    curl -s -o /dev/null -w "HTTP_CODE:%{http_code}\n" -m 8 "https://$host"
  done

  ESPERADO: HTTP_CODE diferente de 000 e diferente de 403 para os 3.
  (000 ou "CONNECT tunnel failed" = proxy da sessão está bloqueando.
   Nesse caso, rode: curl -sS "$HTTPS_PROXY/__agentproxy/status"
   e confira "recentRelayFailures" — se aparecer "connect_rejected"
   para esses hosts, PARE e reporte ao usuário. Não adianta trocar
   a config do ambiente numa sessão já iniciada.)

  Se os 3 hosts responderem (mesmo que com erro HTTP tipo 401, 404),
  a rede está OK — prossiga para o Passo 1.


================================================================
REGRA DE OURO — NUNCA COMMITAR SEGREDOS
================================================================

Um erro grave já aconteceu numa sessão anterior: as chaves reais
(SUPABASE_SERVICE_ROLE_KEY, GROQ_API_KEY, COHERE_API_KEY) foram
escritas em texto plano neste mesmo arquivo e comitadas em
main + na branch de testes. Foram corrigidas (redigidas) depois,
mas o vazamento ficou no histórico do git.

REGRAS PARA ESTA SESSÃO:
  - NUNCA escrever valores reais de chave/token em qualquer arquivo
    dentro do repositório (nem .txt, nem .md, nem comentário de código).
  - As chaves só devem existir em `.env.local` (que está no
    .gitignore) e/ou em variáveis de shell (export TOKEN=...),
    nunca em arquivos rastreados pelo git.
  - Se o usuário colar uma chave no chat, use-a para criar/editar
    `.env.local` e não a reproduza depois em texto solto.
  - Ao terminar os testes, se as chaves foram compartilhadas via
    chat nesta ou em sessão anterior, recomende ao usuário rotacioná-las
    (Supabase: Project Settings → API; Groq: console.groq.com;
    Cohere: dashboard.cohere.com).


================================================================
PARTE 1 — PREPARAÇÃO
================================================================

PASSO 1 — Branch e instalação
  git checkout claude/kronia-nurse-library-tests-yreho1
  npm install

PASSO 2 — Criar .env.local na raiz do projeto
  Precisa de 6 variáveis. Para obter os valores:

  NEXT_PUBLIC_SUPABASE_URL       → mcp Supabase get_project_url (project_id: uguxeoftfnljrxhwvdkj)
  NEXT_PUBLIC_SUPABASE_ANON_KEY  → mcp Supabase get_publishable_keys (usar a key "anon"/legacy, tipo JWT)
  SUPABASE_URL                   → igual à NEXT_PUBLIC_SUPABASE_URL
  SUPABASE_SERVICE_ROLE_KEY      → pedir ao usuário (MCP não expõe service_role por segurança)
  GROQ_API_KEY                   → pedir ao usuário
  COHERE_API_KEY                 → pedir ao usuário

  Escrever esses 6 valores em .env.local (arquivo, não commitar).
  Se qualquer variável estiver ausente → PARAR e reportar quais faltam.

PASSO 3 — Criar usuário de teste e obter TOKEN
  (usar as variáveis do .env.local via shell, nunca hardcoded em arquivo)

  set -a; source .env.local; set +a
  TESTEMAIL="teste.biblioteca.$(date +%s)@kronianurse.test"
  TESTPASS="<gerar senha aleatória forte em memória>"

  curl -s -X POST "$SUPABASE_URL/auth/v1/admin/users" \
    -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
    -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"$TESTEMAIL\",\"password\":\"$TESTPASS\",\"email_confirm\":true}"

  TOKEN=$(curl -s -X POST "$SUPABASE_URL/auth/v1/token?grant_type=password" \
    -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY" \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"$TESTEMAIL\",\"password\":\"$TESTPASS\"}" | jq -r '.access_token')

  echo "TOKEN obtido: $([ -n "$TOKEN" ] && echo OK || echo FALHOU)"
  Se TOKEN for null/vazio → PARAR e reportar erro completo (sem vazar a senha gerada).

PASSO 4 — Subir servidor
  npm run dev &
  sleep 6
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000
  ESPERADO: 200. Se não vier 200 → PARAR e reportar log do servidor.


================================================================
PARTE 2 — BLOCO 0 (já validado, apenas reconfirmar rápido)
================================================================

Via MCP Supabase execute_sql (project_id: uguxeoftfnljrxhwvdkj):
  SELECT table_name FROM information_schema.tables
  WHERE table_schema='public' AND table_name LIKE 'knowledge%' ORDER BY table_name;
  → esperado 5 tabelas.

Se passar, marcar Bloco 0 = PASS e seguir para Bloco 1.


================================================================
PARTE 3 — BLOCO 1: Pipeline para 3 temas (~3–6 min)
================================================================

curl --max-time 600 -s -X POST http://localhost:3000/api/kronos/biblioteca/processar \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"temas":["sonda vesical de demora","oxigenoterapia por cateter nasal","coleta de hemocultura"]}' \
  | jq . | tee /tmp/bloco1_resultado.json

CRITÉRIO DE PARADA:
  "ok" = true
  "sucessos" = 3
  "erros" = 0
  todos "spec_id" são UUIDs não-nulos
  todos "status" são "aguardando_aprovacao" ou "reprovado" (nunca "rascunho")
  todos têm "classificacao" preenchida (verde/amarelo/vermelho)

Se qualquer critério falhar → PARAR e reportar o JSON completo de
/tmp/bloco1_resultado.json. Não avançar para o Bloco 2.

Guardar:
  SPEC_ID_1 = spec_id do tema "sonda vesical de demora"
  SPEC_ID_2 = spec_id do tema "oxigenoterapia por cateter nasal"
  SPEC_ID_3 = spec_id do tema "coleta de hemocultura"


================================================================
PARTE 4 — BLOCOS 2–9
================================================================

Seguir na íntegra o arquivo supabase/KRONIA_NURSE_TESTES_BIBLIOTECA.txt
a partir do Bloco 2, usando $TOKEN, $SPEC_ID_1, $SPEC_ID_2, $SPEC_ID_3
obtidos acima. Esse arquivo tem o detalhamento completo de SQL,
curl e critérios PASS/FAIL para cada bloco — não resumir nem pular
etapas.

Resumo executivo dos critérios (não substitui o arquivo completo):
  Bloco 2 — 3 specs no banco, 6 audit entries, knowledge_base vazio, refs >= 2
  Bloco 3 — UI mostra 3 grupos (verde/amarelo/vermelho), checkbox funciona
  Bloco 4 — SPEC_1 aprovada (→knowledge_base), SPEC_2 reprovada, SPEC_3 pendente
  Bloco 5 — knowledge_base tem 1 linha com embedding não-nulo e spec_id correto
  Bloco 6 — KRONOS responde sobre SPEC_1 com citação; não vaza SPEC_2 rejeitada
  Bloco 7 — 401 sem token, 404 uuid inválido, 400 status errado, 400 >10 temas, 0 linhas vazadas
  Bloco 8 — 2 temas extras aprovados em lote; embedding gerado para ambos
  Bloco 9 — plantão/escalas/auth/legado funcionam; npx tsc --noEmit = 0 erros


================================================================
PARTE 5 — RESTRIÇÕES ABSOLUTAS (Constitution)
================================================================

Durante toda a execução dos testes, NUNCA:
  1. Criar caminho que escreva em knowledge_base sem /api/knowledge-spec/aprovar
  2. Gerar embedding fora de aprovar.ts
  3. Aprovar spec com pipeline_classificacao='vermelho'
  4. Aprovar spec com status != 'aguardando_aprovacao'
  5. Fazer KRONOS ler de knowledge_specs diretamente
  6. Commitar qualquer valor real de chave/token/senha no repositório

Se qualquer teste exigir violar uma dessas regras → é FAIL do teste,
não uma exceção a ser implementada.


================================================================
PARTE 6 — RESULTADO CONSOLIDADO (preencher ao final)
================================================================

| Bloco      | Descrição                              | Status        |
|------------|-----------------------------------------|---------------|
| Passo 0    | Teste de rede (3 hosts)                | [ ] PASS/FAIL |
| TypeScript | npx tsc --noEmit                       | [ ] PASS/FAIL |
| Bloco 0    | Schema: 5 tabelas, 16 cols, RLS, RPC   | [PASS]        |
| Bloco 1    | Pipeline 3 temas → specs criadas       | [ ] PASS/FAIL |
| Bloco 2    | SQL pós-pipeline                       | [ ] PASS/FAIL |
| Bloco 3    | UI /biblioteca-tecnica                 | [ ] PASS/FAIL |
| Bloco 4    | Aprovar SPEC_1, reprovar SPEC_2        | [ ] PASS/FAIL |
| Bloco 5    | SQL pós-decisão (KB + embedding)       | [ ] PASS/FAIL |
| Bloco 6    | KRONOS RAG + não-vazamento             | [ ] PASS/FAIL |
| Bloco 7    | 5 negative tests (7A–7E)               | [ ] PASS/FAIL |
| Bloco 8    | 2 temas extras aprovados em lote       | [ ] PASS/FAIL |
| Bloco 9    | Não-regressão + tsc=0 erros            | [ ] PASS/FAIL |

Observações / bugs encontrados:
  [preencher aqui com erros reais, spec_ids, KB_IDs, JSONs de falha]

Declarar "entregue e funcional" somente quando todos marcarem PASS.


================================================================
PARTE 7 — ARQUIVOS DE REFERÊNCIA
================================================================

Implementação:
  lib/knowledge-pipeline.ts                 Etapas 1–8 (pesquisar + redigir + auditores)
  lib/auth-server.ts                        valida Bearer token via supabase.auth.getUser
  pages/api/kronos/biblioteca/processar.ts  pipeline por tema (NUNCA escreve em KB)
  pages/api/knowledge-spec/aprovar.ts       ÚNICA rota que publica em knowledge_base
  pages/biblioteca-tecnica.tsx              UI completa

Banco:
  supabase/KRONIA_NURSE_SQL_COMPLETO.txt    SQL para aplicar no Supabase (se precisar recriar)

Testes detalhados:
  supabase/KRONIA_NURSE_TESTES_BIBLIOTECA.txt   protocolo completo com todos os critérios

Handoff anterior (histórico, contexto adicional):
  supabase/KRONIA_NURSE_PROMPT_CONCLUSAO.txt

================================================================
FIM DO PROMPT MASTER DE TESTES — SESSÃO COM REDE HABILITADA
================================================================
